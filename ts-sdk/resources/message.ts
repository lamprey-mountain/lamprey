// TODO
// frontend/src/api/services/MessagesService.ts

// async send(channel_id: string, body: MessageSendReq): Promise<Message> {

export class MessagesManager {
	// channel
	// private cache
	//
	// send()
	// fetch()
	// listAfter()
	// listBefore()
	// listContext()
}

export class Message {
	// reactions: ReactionsManager
	// update()
	// delete()
}

export class ReactionsManager {
	// channel
	// message
	// all(): array reactioncount
	// get(key): reactioncount
	// create(key)
	// delete(key)
	// deleteUser(user, key)
	// deleteKey(key)
	// deleteAll()
	// collect()
}
